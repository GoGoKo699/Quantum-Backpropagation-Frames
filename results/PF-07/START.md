# PF-07 start checkpoint

Status: IN PROGRESS. Date: 2026-09-16.
The user's instruction to proceed authorizes the overlapping-support compiler study recommended after PF-06.

Baseline: 0633ff37de69debbad93b78b146bb128be12106e (research/PF-06-sparse-compiler).
Working branch: research/PF-07-overlap-reference.
Main remains at 1986fe53a65062120f1be9da14c28fa48b12484d.

## Bounded task and predeclared checks

Extend the known row-norm reference readout to real tangents supported on overlapping intervals, using the existing alternating six-rotation circuit family. Construct the reference from the charged local tangent tables without storing a global address-support union or obtaining a tangent eigenbasis for free. Investigate a unique first-nonzero-site decomposition: prepare one marker, then fill its bounded suffix in descending marker order. All controls, elementary gates, classical preprocessing, physical bit acquisition, sparse decoding, memory and output must be counted.

Fixed small grid: n=3,4,5,6,8, block-layer depth d=1,2,3, with zero and seeded nonzero angles. New Born checks use explicit gate execution, not only the proposed formula. Include the exact PF-06 overlapping-layer counterexample, invalid construction order, zero rows, mixed-width and synthetic interval tables, and independent dense gradients. Compiler-only scaling uses n=16,32,64,128,256 at d=ceil(log2(n)/2); a separate fixed-depth d=2 sequence uses the same sizes. No large global state is allocated.

Separate a general variance bound from the inherited flat-spectrum optimum. Keep original raw coordinates and whole-vector accuracy/confidence fixed. Include optimized no-mask/mask and local-shadow baselines; no favorable-objective or price-winner search. Sparse state preparation and conditional-amplitude synthesis receive a bounded primary-source comparison, not a novelty certificate.

Preserve all prior results, code, archives, tests and branch heads. Add a self-contained proof, compiler, diagnostics, operation ledger and decision here. Run local checks and read-only GitHub validation. Do not merge, release, select a license, draft a manuscript, prove an arbitrary-spectrum optimum, or launch a follow-on study.
